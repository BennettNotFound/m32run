[package]
name = "m32run"
version = "0.1.0"
edition = "2021"

[dependencies]
loader = { path = "../loader" }
x86core = { path = "../x86core" }
shim = { path = "../shim" } # 新增这一行
host = { path = "../host" }
thiserror = "1"
plist = "1"
runtime_log = { path = "../runtime_log" }
ctrlc = "3"
